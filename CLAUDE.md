## Agent skills

### Issue tracker

GitHub Issues in `ajorquera/new` (uses the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Agent run pipeline

How labeling an issue/PR triggers an agent run, and the split between this repo and `ajorquera/agent-runner`. See `docs/agents/agent-run-pipeline.md`.
